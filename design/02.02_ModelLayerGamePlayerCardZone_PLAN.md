# 02.02 ModelLayerGamePlayerCardZone Technical Plan

> 本文档对应《三国杀 Core 规则引擎模块拆解（core_module_breakdown）》中的：
> **章节 2. 模块拆分与推荐开发顺序 → 小节 1. 基础设施与模型层 → 第 2 项：模型层：Game / Player / Card / Zone（纯状态，几乎无外部依赖）**。
>
> 模型代码实现请参考 `LegendOfThreeKingdoms.Core.Model` 命名空间及其子命名空间（如 `LegendOfThreeKingdoms.Core.Model.Zones`）。

本模块的详细技术设计（字段、职责、目录与命名空间规划）请参考 `.cursor/plans/02-02-model-layer-game-player-card-zone-plan_*.plan.md`，以避免在仓库中重复维护同一份长文档。
