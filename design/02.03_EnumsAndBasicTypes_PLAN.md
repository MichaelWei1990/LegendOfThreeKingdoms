# 02.03 EnumsAndBasicTypes Technical Plan

> 本文档对应《三国杀 Core 规则引擎模块拆解（core_module_breakdown）》中的：
> **章节 2. 模块拆分与推荐开发顺序 → 小节 1. 基础设施与模型层 → 第 3 项：枚举与基础类型定义**。
>
> 枚举与基础类型的代码实现请参考 `LegendOfThreeKingdoms.Core.Model` 命名空间下的 `core/Model/Enums.cs`，以及依赖这些枚举的 `Card` / `Game` / `Player` 等模型类型。

本模块的详细技术设计（字段、职责、目录与命名空间规划）请参考 `.cursor/plans/02-03-enums-and-basic-types-plan_*.plan.md`，以避免在仓库中重复维护同一份长文档。
